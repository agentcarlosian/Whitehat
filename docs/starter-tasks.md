# Starter contribution candidates

These are scoped proposals, not claims of implemented support or assigned work.

| Task | Acceptance criteria |
| --- | --- |
| Improve one rule explanation | Explain actor-controlled reachability and a concrete negative control; preserve existing fingerprints |
| Add a TypeScript fixture/profile | Pin actual engine behavior, prove vulnerable/fixed twins, document parsing limits and native platform results |
| Improve advisory alias presentation | Group duplicate advisory aliases per exact package/version while preserving conflicts and original IDs |
| Add SARIF secondary-location display | Use explicit data from the report, avoid external resolution, add path/redaction controls |
| Add a macOS native profile | Verify official asset and member hashes, run the complete native evaluation on macOS, update compatibility docs |

Open a focused issue before substantial work so the maintenance scope is clear.
